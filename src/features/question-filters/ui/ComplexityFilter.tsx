interface ComplexityFilterProps {
	value?: number[]
	onChange: (value: number[]) => void
}

export const ComplexityFilter = ({
	value = [],
	onChange
}: ComplexityFilterProps) => {
	// Твоя реализация с чекбоксами или кнопками
	return <div>...</div>
}
